columns:
  - type: pk
  - name: route_id
    type: fk
    isUnique: true
    references:
      table: route
      column: id
      onDelete: CASCADE
  - name: screen_id
    type: fk
    isUnique: true
    references:
      table: screen
      column: id
      onDelete: CASCADE
  - type: created_at
  - type: updated_at

columns:
  - type: pk
  - name: menu_id
    type: fk
    isUnique: true
    references:
      table: menu
      column: id
      onDelete: CASCADE
  - name: screen_id
    type: fk
    isUnique: true
    references:
      table: screen
      column: id
      onDelete: CASCADE
